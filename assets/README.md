# Shared sample assets

Sample collectibles, coordinates, and config shared across the React Native,
Swift, and Kotlin sample apps so the three behave **visibly identically** (a key
parity check — see `seekar-spec` §10).

> Placeholder. Populated in Phase 4 (examples finalization) with:
> - test collectible definitions (model URLs, titles, anchor types),
> - a small set of test coordinates,
> - any shared particle/texture overrides used for demos.

Until then, each app uses its own local fixtures (e.g. the React Native sample's
`src/` data).
